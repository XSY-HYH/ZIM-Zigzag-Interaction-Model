# CHAP‑IEM‑CSW 技术文档

## 针对 CHAP‑IEM 的并发滑动窗口扩展

> **注意：** 本协议**不是**传统的挑战‑握手认证协议（CHAP）。它是链式哈希认证协议（Chain Hash Authentication Protocol）ID 加密模式（CHAP‑IEM）的一个扩展。

---

## 一、概述

CHAP‑IEM‑CSW（Concurrent Sliding Window，并发滑动窗口）是 **CHAP‑IEM** 的一个扩展，它在**完全保留前向安全性和抗重放能力**的前提下，实现了**异步并发请求**。

- **解决的核心问题：** 原始 CHAP‑IEM 强制顺序执行（请求 N 必须收到响应 N 后才能发送请求 N+1）。这阻碍了 HTTP/2 多路复用、并行 API 调用以及任何高并发场景。
- **解决方案：** 为每个会话引入**多个独立的逻辑通道**，每个通道拥有自己的 ID 链和一个用于请求编号的**滑动窗口**。客户端可以**预先生成下一个 ID**，并在任意通道上发送请求，无需等待之前的响应。
- **保留的安全属性：** CHAP‑IEM 的所有优点均保留 —— 前向安全、抗重放攻击、自动恢复（通过原始的 K / K_session 通道）。

---

## 二、与原始 CHAP‑IEM 的对比

| 特性                      | 原始 CHAP‑IEM           | CHAP‑IEM‑CSW                     |
|---------------------------|-------------------------|----------------------------------|
| 异步并发                  | ❌ 不支持               | ✅ 支持（多通道 + 窗口）         |
| 每个通道的请求顺序        | 严格顺序                | 通道内顺序，但通道间并行         |
| ID 生成方                 | 服务端分配              | 客户端生成（预发送）             |
| 抗重放                    | 单次 ID 失效            | 序列号 + ID 滑动窗口             |
| 前向安全                  | ✅ 支持                 | ✅ 支持                          |
| 异常恢复                  | 通过 K / K_session      | 相同 + 支持单通道重置            |
| 适用场景                  | 低并发、顺序关键的操作  | 高并发、现代 Web / API           |

---

## 三、核心概念

### 3.1 多通道（C‑channels）

- 一个会话由 **C 个独立的逻辑通道**组成（例如 C = 32）。
- 每个通道 `ch` 维护自己独立的 **ID 链**：  
  `ID_ch0`（初始）→ `ID_ch1` → `ID_ch2` → ...
- 通道数量 `C` 在登录阶段协商确定（整个会话固定）。
- 通道之间相互独立：通道 3 上的丢包不会阻塞通道 7。

### 3.2 客户端生成下一个 ID

- 在原始 CHAP‑IEM 中，服务端在响应中返回 `ID_n+1`。
- 在 CHAP‑IEM‑CSW 中，**客户端在本地生成**下一个 ID（`next_id`）（必须为 CSPRNG，256 位），并将其**放入请求中**发送给服务端。
- 发出请求后，客户端**立即**将该通道的本地密钥更新为 `next_id`。无需等待服务端确认（乐观更新）。
- 服务端验证 `next_id`，如果有效，则相应地更新自己在该通道上的状态。

### 3.3 序列号与滑动窗口

每个通道维护：

- `seq_num`（32 位无符号整数，从 0 开始）—— 请求计数器。
- 发送方（客户端）每发送一个新请求，就递增该通道的 `seq_num`。
- 接收方（服务端）维护：
  - `expected_seq[ch]` —— 下一个期望的序列号。
  - 一个**滑动窗口**，记录最近使用过的 `(seq_num, next_id)` 对（窗口大小 W，例如 64）。
- 同一通道上的请求**必须按递增的 seq_num 顺序发送**（不允许跳号）。  
  *这使得服务端能够检测重放或乱序的数据包。*

### 3.4 数据包格式

**登录阶段**（与 CHAP‑IEM 相同，但携带多个初始 ID）

客户端 → 服务端：`AES256_K(用户名)`

服务端 → 客户端（如果成功）：  
`AES256_K( "OK" || ID_0_0 || ID_1_0 || ... || ID_{C-1}_0 )`

其中每个 `ID_ch_0` 都是一个全新的 256 位随机值。

**操作请求**（客户端 → 服务端）

```text
请求 = {
    ch : uint16,           // 通道编号 0..C-1
    seq : uint32,          // 该通道的序列号
    next_id : bytes(32),   // 下一个 ID，将作为该通道下一次请求的密钥
    encrypted_payload : bytes   // 实际指令（用该通道的当前 ID 进行 AES256 加密）
}
```

加密：使用 `AES256_{ 该通道的当前 ID }` 加密整个（ch || seq || next_id || payload）。

**操作响应**（服务端 → 客户端）

```text
响应 = {
    ch : uint16,
    seq : uint32,          // 与请求中的 seq 相同
    status : uint16,       // 成功/错误码
    result : bytes         // 用**解密该请求所用的同一个 ID** 加密
}
```

重要：服务端**不再返回新的 ID** —— 客户端已经知道下一个 ID（`next_id`）。

---

## 四、协议流程详解

### 4.1 登录与通道建立

- 客户端与服务端预共享密钥 K（或完成 SKN 密钥交换）。
- 客户端发送 `AES256_K(用户名)`。
- 服务端验证，生成 C 个 256 位随机 ID：`ID_0_0, ID_1_0, ..., ID_{C-1}_0`。
- 服务端回复 `AES256_K( OK || ID_0_0 || ID_1_0 || ... || ID_{C-1}_0 )`。
- 客户端解密后存储 `current_ID[ch] = ID_ch_0`（所有 ch）。
- 双方初始化 `expected_seq[ch] = 0` 并清空滑动窗口。

### 4.2 正常并发请求

假设客户端想要在通道 0 和通道 1 上**同时**发送两个独立的指令。

**通道 0 请求：**

- 客户端取当前 `ID = current_ID[0]`（即 `ID_0_0`）。
- 生成新的随机 `next_id0`。
- 用 `ID_0_0` 加密 `(ch=0, seq=0, next_id0, cmd_A)`。
- 发送数据包。
- 立即将 `current_ID[0]` 更新为 `next_id0`，并将本地通道 0 的 seq 计数器递增到 1。

**通道 1 请求（立即发送，无需等待）：**

- 使用 `current_ID[1] = ID_1_0`，生成 `next_id1`，加密 `(ch=1, seq=0, next_id1, cmd_B)`。
- 发送，更新 `current_ID[1] = next_id1`，本地 seq 变为 1。

**服务端处理（可以是任意顺序）：**

- 对于通道 0 请求：用 `ID_0_0` 解密，检查 `seq == expected_seq[0] (0)`，将 `(seq=0, next_id0)` 存入滑动窗口，执行 `cmd_A`，更新 `expected_seq[0]=1` 和 `current_ID[0] = next_id0`。
- 对于通道 1 请求：同样独立处理。
- 响应返回，每个响应用**解密该请求时所用的同一个 ID** 加密（即通道 0 响应用 `ID_0_0`，通道 1 响应用 `ID_1_0`）。

### 4.3 抗重放攻击

攻击者截获了用 `ID_0_0` 加密的数据包 `(ch=0, seq=0, next_id0, cmd_A)`，稍后重放。

- 此时服务端的 `expected_seq[0]` 已经是 1（或更大）。
- 收到的 `seq=0` 小于期望值 → **拒绝**（旧包/乱序）。
即使 `expected_seq` 意外匹配（这在一个有效请求后绝不会发生），滑动窗口也会记住 `(seq=0, next_id0)` 已经被使用过，从而拒绝重复。

### 4.4 同一通道上的乱序到达

由于网络延迟，同一通道上的请求 `seq=1` 可能比 `seq=0` 先到达。

- 服务端的 `expected_seq[ch]` 仍是 0。
- 收到 `seq=1` 的数据包 → `seq` > 期望值 → 服务端可以**缓存**它（如果实现缓存），或者直接拒绝。
  *建议：* 服务端可以维护一个小型的乱序缓冲区（大小等于窗口），等待缺失的 seq 到达后按顺序处理。
  另一种做法：客户端确保每个通道严格按序发送（例如使用单个发送队列）。

### 4.5 滑动窗口维护

服务端为每个通道维护一个固定大小的窗口（例如 64 个条目），记录最近看到的 `(seq, next_id)` 对。

- 当处理一个新的有效请求 `(seq, next_id)` 时：
  - 将 `(seq, next_id)` 加入窗口。
  - 删除比 `seq - W` 更旧的条目（W 为窗口大小）。
- 在接收请求前进行重放检测：
  - 如果 `seq` 已经存在于窗口中 → 拒绝。
  - 如果 `seq` < `expected_seq[ch]` 但不在窗口中找到 → 也拒绝（该包太旧且已被移出窗口，视为重放）。

### 4.6 异常恢复

两种故障类型：

**1. 响应丢失**（客户端发送了请求，服务端处理了，但响应从未到达）

- 客户端的该请求超时。
- 由于客户端已经将 `current_ID[ch]` 推进到了 `next_id`，它**无法**在同一个通道上重传相同的请求（旧的密钥已销毁）。
- **解决方案：** 客户端打开一个**新通道**（如果有剩余通道），或者使用原始的基于 K / K_session 的恢复机制来重置整个会话（或重置受影响的通道）。
  *更简单的做法：* 客户端将丢失响应的请求视为通道故障，将后续对话迁移到新通道上，让失效通道被垃圾回收。

**2. 服务端不同步**（客户端丢失了一个响应，然后尝试使用服务端不再接受的 ID 发送下一个请求）

- 服务端收到一个请求，其 `current_ID` 与该通道上存储的 `current_ID[ch]` 不匹配。
- 服务端回退到原始 CHAP‑IEM 的恢复方式：在恢复通道上发送 `AES256_K("resync" || 当前有效ID || ...)`。
- 客户端使用 K（或 K_session）恢复，与标准 CHAP‑IEM 一致。

---

## 五、安全性分析

### 5.1 重放攻击

- 攻击者无法重放过期的请求，因为：
  - 序列号必须严格递增。
  - 每个 `(seq, next_id)` 对都存储在滑动窗口中；重复项会被拒绝。
  - 即使序列号空间回绕（2³² 次操作），窗口机制也能确保超出窗口范围的旧包因其 `seq` < `expected_seq - W` 且不在窗口中而被拒绝。

### 5.2 前向安全

- 每个请求都使用全新的加密密钥（`current_ID`）。
- 旧密钥在使用后立即销毁（服务端在更新后，客户端在发送后）。
- 后续密钥被泄露也无法解密之前的通信，因为之前的密钥无法被推导出来。

### 5.3 客户端生成 ID 的安全性

- ID 必须使用 CSPRNG 生成（256 位）。
- 可预测的 ID 将允许攻击者猜测未来的密钥。
- 服务端**必须**验证 `next_id` 是全新的（未被使用过）并且看起来随机（除了去重，无需显式检查随机性）。

### 5.4 通道独立性

- 攻陷一个通道不会影响其他通道（不同的 ID 链）。
- 然而，恢复密钥 K（或 K_session）是所有通道共享的 —— 如果 K 泄露，攻击者可以重置任意通道。这可以接受，因为 K 是根密钥。

### 5.5 通道数量限制（C）

- C 应该是一个固定的、适中的数字（如 32、64、128）。
- 通道过多会增加服务端状态，并可能被滥用（通道耗尽攻击）。
- 服务端**应该**实现空闲通道回收。

---

## 六、实现指南

### 6.1 随机数生成

- 所有 ID（`ID_ch0`、`next_id`）以及任何随机值**必须**使用密码学安全伪随机数生成器（CSPRNG）生成。
- 不得使用顺序数、时间戳或低熵值。

### 6.2 加密算法

- 与 CHAP‑IEM 相同：
  - 推荐 AES‑256 GCM 模式，或 CBC 模式配合 HMAC‑SHA‑256。
  - 如果只能使用 CBC，则依赖 ID 链提供完整性保护，并使用相同的错误响应以避免填充预言机攻击。

### 6.3 滑动窗口大小（W）

- 推荐 W = 64。
- 更大的窗口可以容忍更多乱序包，但会增加内存占用。
- W 至少应等于最大期望的乱序深度。

### 6.4 通道数量（C）

- 建议默认 C = 32。
- 可以在登录时协商（客户端在登录请求中发送 `C`，服务端按自身配置设置上限）。

### 6.5 超时与重传

- 客户端应该实现每个请求的超时机制。
- 如果响应未到达，客户端**不应该**在同一个通道上重传相同的请求（因为密钥已经更新）。替代方案：
  - 使用另一个可用通道发送相同的逻辑指令。
  - 或者触发完整的会话恢复（通过 K）。

### 6.6 内存管理

- 服务端**必须**限制每个通道的滑动窗口和乱序缓冲区的大小，以防止内存耗尽攻击。
- 空闲通道应在可配置的超时时间（例如 5 分钟）后重置。

---

## 七、适用场景

CHAP‑IEM‑CSW 特别适用于：

- **HTTP/2、HTTP/3、gRPC**：在一个加密会话内实现多个流的并发。
- **实时多人游戏**：大量小型独立动作（移动、聊天、背包）并发发送。
- **物联网命令管道**：传感器读数和控制命令异步到达。
- **带有并行 API 调用的 Web 前端**：一个会话处理多个同时的 fetch/XHR 请求。

**不推荐**的场景：

- 极低功耗的嵌入式设备，一次只需要一个请求（为了简单使用原始 CHAP‑IEM）。
- 当网络顺序要求严格且并发无益时。

---

## 八、总结

CHAP‑IEM‑CSW 扩展了原始 CHAP‑IEM 协议，使其能够支持**异步并发请求**，同时不牺牲任何安全属性。通过引入：

- **多个独立的通道**，
- **客户端生成的滚动 ID**，
- **带滑动窗口的序列号抗重放保护**，

它解除了严格的顺序瓶颈，使得现代高性能通信模式成为可能，同时保留了前向安全、轻量级实现和自动恢复能力。

该协议与 ZIM（之字形交互模型）完全兼容 —— 每个请求仍然推进状态；只是现在有多个状态链可以并行推进。

---

## 九、参考文献

- CHAP‑IEM.md —— 原始 ID 加密模式规范
- pih.md —— 性能优化指南
- BCPF.md —— 双向通信模式
- ZIM（Zigzag Interaction Model）—— 理论基础

*文档版本：1.0*
