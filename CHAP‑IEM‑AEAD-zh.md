# CHAP‑IEM‑AEAD 技术文档

## 为 CHAP‑IEM 增加认证加密与显式 Nonce 支持

> **注意：** 本协议**不是**传统的挑战‑握手认证协议（CHAP）。它是链式哈希认证协议（Chain Hash Authentication Protocol）ID 加密模式（CHAP‑IEM）的一个安全强化变体。

---

## 一、概述

CHAP‑IEM‑AEAD 是在原始 CHAP‑IEM 基础上，强制使用 **认证加密（AEAD）** 并引入 **显式 nonce / 序列号管理** 的安全增强变体。

**解决的问题：**

| 问题 | 原始 CHAP‑IEM | CHAP‑IEM‑AEAD |
|------|----------------|----------------|
| 加密模式 | 未指定（CBC 存在填充预言机风险） | 强制 AEAD |
| 完整性保护 | 依赖 ID 不可预测性间接保证 | 认证加密直接保证 |
| Nonce 管理 | 无（实现者责任） | 协议规定显式 nonce |
| 抗重放 | 仅 ID 一次性 | ID 一次性 + 序列号单调检查 |

**解决方案：**

- 强制使用 **AES‑256‑GCM** 或 **ChaCha20‑Poly1305** 认证加密
- 每个数据包携带显式 12 字节 nonce，其中内嵌单调递增序列号
- 保证 `(密钥, nonce)` 对永远不重复
- 接收方检查序列号单调性，与 ID 一次性机制形成 **双重抗重放**
- 保留 CHAP‑IEM 全部优点：前向安全、自动恢复、ID 作为密钥、中间人防护

---

## 二、与原始 CHAP‑IEM 的对比

| 特性 | 原始 CHAP‑IEM | CHAP‑IEM‑AEAD |
|------|----------------|----------------|
| 加密模式 | 未指定（CBC 填充风险） | 强制 AEAD（GCM 或 Poly1305） |
| 完整性保护 | 依赖 ID 不可预测性间接保证 | 认证加密直接保证 |
| Nonce 管理 | 无（用户责任） | 协议规定：12 字节显式 nonce，内含序列号 |
| 抗重放 | 仅 ID 一次性 | ID 一次性 + 序列号单调检查 |
| 每包额外开销 | 0 字节 | 12 字节 nonce + 16 字节 tag |
| 前向安全 | ✅ | ✅ |
| 自动恢复 | ✅ | ✅ |
| 中间人防护 | ✅（通过预共享 K） | ✅（通过预共享 K） |

---

## 三、核心协议设计

### 3.1 加密算法

- **AEAD 算法**：AES‑256‑GCM（推荐）或 ChaCha20‑Poly1305
- **密钥长度**：256 位（ID 本身即密钥，需保证 256 位熵）
- **Nonce 长度**：12 字节（96 位），协议显式传输

### 3.2 Nonce 构造规则

每个会话（或每个通道，为将来 CSW 风格并发预留）独立维护两个 **64 位发送计数器**：

- `client_send_seq`：客户端发出的请求包计数器
- `server_send_seq`：服务端发出的响应包计数器

**Nonce 格式（12 字节）：**

```
nonce = [ channel_id (4 字节) ][ sequence_number (8 字节) ]
```

- `channel_id`：当前通道标识（单通道时恒为 0）
- `sequence_number`：发送计数器值（大端序）

每个包发出后，对应方向的计数器加 1。由于每个密钥 `ID_n` 仅用于一个请求和一个响应，且计数器单调递增，因此 `(密钥, nonce)` 对永不重复，完全满足 AEAD 安全要求。

### 3.3 包格式

#### 登录阶段（与 CHAP‑IEM 相同，但换用 AEAD）

**客户端 → 服务端：**

```
[ 12 字节 nonce ][ AES256‑GCM_K( 用户名 ) ][ 16 字节 tag ]
```

**服务端 → 客户端（成功时）：**

```
[ 12 字节 nonce' ][ AES256‑GCM_K( "OK" || ID₁ ) ][ 16 字节 tag ]
```

#### 操作阶段（密钥已切换为 IDₙ）

**请求包（客户端 → 服务端）：**

```
[ 12 字节 nonce ][ ciphertext ][ 16 字节 tag ]
```

其中：
- `ciphertext = AEAD_encrypt( key = current_ID, nonce, plaintext )`
- `plaintext = 操作指令`（原始 IEM 中请求不携带 next_id）

**响应包（服务端 → 客户端）：**

```
[ 12 字节 nonce' ][ ciphertext' ][ 16 字节 tag' ]
```

其中：
- `plaintext' = 操作结果 || ID_{n+1}`

客户端用相同的 `current_ID`（即请求时使用的 IDₙ）解密响应，获得 `ID_{n+1}`，然后更新密钥。

> **注意：** 与 CSW 不同，此处仍遵循原始 IEM 的“服务端分配下一个 ID”模式。请求中不包含客户端预生成的 next_id。

### 3.4 序列号检查规则

双方各自维护接收计数器：

| 维护方 | 变量 | 初始值 |
|--------|------|--------|
| 客户端 | `last_recv_server_seq` | -1 |
| 服务端 | `last_recv_client_seq` | -1 |

收到包后，提取 nonce 中的 `sequence_number`：

- 客户端收到响应：必须 `seq > last_recv_server_seq`，否则丢弃
- 服务端收到请求：必须 `seq > last_recv_client_seq`，否则丢弃

检查通过后，更新对应的 `last_recv_*_seq`。

**作用和意义：** 即使攻击者重放一个旧密文（且 ID 恰好因为某些异常窗口未被销毁），序列号检查仍然会拒绝，形成第二道防线。

### 3.5 与原始 IEM 自动恢复的兼容

异常恢复机制保持不变。当服务端检测到 ID 无效时，使用预共享密钥 K（或 SKN 中的 K_session）加密恢复包：

```
恢复包 = [ nonce ][ AES256‑GCM_K( "resync" || current_valid_ID ) ][ tag ]
```

恢复包也遵循 AEAD 格式，携带 nonce（使用 K 下的独立计数器）。客户端解密后更新密钥。

---

## 四、安全性分析

### 4.1 完整性保护

原始 IEM 若仅使用 CBC 模式，攻击者可能篡改密文并利用解密结果或填充错误 oracle 获取信息。AEAD 模式确保任何对密文的篡改都会导致解密失败（tag 不匹配），直接丢弃。

### 4.2 抗重放

| 层级 | 机制 | 说明 |
|------|------|------|
| 第一层 | ID 一次性 | 服务端销毁旧 ID，重放请求因 ID 无效被拒 |
| 第二层 | 序列号单调检查 | 即使 ID 状态意外残留，序列号也会暴露重放 |

**双重保护**，远超原始 IEM 和 TLS/SSH 的单层序列号机制。

### 4.3 Nonce 重用预防

协议强制使用单调递增的 64 位序列号作为 nonce 的一部分，且每个 `ID_n` 仅用于一次请求和一次响应。因此，每个 `(密钥, nonce)` 对全局唯一，彻底杜绝 GCM nonce 重用灾难。

### 4.4 前向安全

与原始 IEM 相同：每次请求/响应后密钥更新为新的随机 `IDₙ₊₁`，旧 ID 立即销毁。获得当前密钥无法反推历史密钥。

### 4.5 中间人攻击

CHAP‑IEM‑AEAD 与原始 CHAP‑IEM 一样，**能够防护中间人攻击**，因为预共享密钥 `K`（或 SKN 模式中的 `K_session`）**永远不会在网络上传输**。

**为什么不已知 K 就无法实施中间人攻击：**

- 攻击者无法解密登录握手包（使用 `K` 加密）
- 攻击者无法伪造有效的登录包（需要 `K` 才能加密）
- 攻击者无法同时与客户端和服务端建立独立的会话

**中间人攻击成功的唯一前提是攻击者已经知道了 `K`。** 如果 `K` 泄露，攻击者可以冒充任意一方——但这是**密钥泄露**问题，而非协议漏洞。密钥保密是用户的责任，与其他对称密钥协议（SSH、TLS‑PSK、WireGuard）一样。

### 4.6 安全特性总结

| 威胁类型 | CHAP‑IEM‑AEAD 防护能力 |
|----------|------------------------|
| 窃听 | ✅（AES‑256 加密） |
| 密文篡改 | ✅（AEAD tag 校验） |
| 重放攻击 | ✅（ID 一次性 + 序列号） |
| Nonce 重用 | ✅（协议强制单调计数器） |
| 填充预言机 | ✅（AEAD 消除 CBC 填充） |
| 中间人攻击 | ✅（需要知道预共享 K） |
| 前向安全 | ✅（ID 链销毁旧密钥） |

---

## 五、工程实现指南

### 5.1 算法选择

| 环境 | 推荐 | 备选 |
|------|------|------|
| 通用（硬件 AES 加速） | AES‑256‑GCM | AES‑256‑GCMSIV（如需 nonce 误用容忍） |
| 低端嵌入式/软件实现 | ChaCha20‑Poly1305 | — |

### 5.2 计数器初始化

客户端和服务端各自的发送计数器可以从 0 开始，也可以选择一个随机初始值（增加一点不可预测性）。接收方检查单调性时，只需比较大小，不要求连续。

### 5.3 错误处理

| 错误类型 | 处理方式 |
|----------|----------|
| 解密失败（tag 校验错误） | 直接丢弃包，可选记录日志 |
| 序列号过小或重复 | 丢弃包，不触发恢复逻辑 |
| ID 无效（但解密成功） | 按原始 IEM 恢复流程处理 |

### 5.4 内存和带宽开销

- 每包额外 **28 字节**（12 nonce + 16 tag）
- 对于大多数命令式交互（每条几十至几百字节），开销可接受
- 若带宽极度紧张，可考虑 ChaCha20‑Poly1305（tag 同样 16 字节）或缩短 nonce 空间（如 8 字节），但不推荐

### 5.5 与 CSW 或 DH 的兼容

本设计可独立使用，也可作为基础组件嵌入到 CSW（多通道）或 DH（匿名）变体中，只需在每个通道独立维护序列号空间即可。

---

## 六、适用场景

**CHAP‑IEM‑AEAD 特别适合：**

- 已经采用 CHAP‑IEM 但担心 CBC 安全性，希望升级到 AEAD 的现有系统
- 要求双重抗重放保障的高敏感环境
- 需要明确 nonce 管理规范、避免实现错误的工程团队
- 能够接受每包 28 字节开销，但无法承受 TLS 全栈复杂性的场景
- 已经依赖 CHAP‑IEM 的中间人防护能力（通过预共享 `K`），同时希望获得更强密码学保证的部署

**不适合：**

- 极端带宽受限（如每秒数万包且每包只有几十字节）
- 预共享密钥分发在操作上不可行的场景（匿名用例请考虑 CHAP‑DH）

---

## 七、总结

CHAP‑IEM‑AEAD 在原始 CHAP‑IEM 基础上强制认证加密和显式序列号管理：

| 改进项 | 效果 |
|--------|------|
| 强制 AEAD | 根治 CBC 填充预言机风险 |
| 显式 nonce + 单调序列号 | 杜绝 GCM nonce 重用漏洞 |
| 双重抗重放 | ID 一次性 + 序列号检查 |
| 明确中间人防护说明 | 文档化预共享 `K` 提供的 MitM 防护能力 |

它保持了 IEM 的全部优势（前向安全、自动恢复、轻量会话状态、预共享密钥提供的中间人防护），同时与现代密码学最佳实践对齐。

该变体可视为 IEM 的 **安全增强配置文件**，适合需要更高可靠性且愿意承担少量开销的部署环境。

---

## 八、协议层次关系

```
ZIM（锯齿交互模型）
  ├── CHAP（固定密钥 + ID 链 + 身份验证）
  ├── CHAP-IEM（ID 作为密钥 + 身份验证）
  ├── CHAP-IEM-SKN（预共享 Y + DH 混合 + 身份验证）
  ├── CHAP-IEM-CSW（并发滑动窗口扩展）
  ├── CHAP-IEM-AEAD（认证加密 + 显式 Nonce）  ← 本文档
  └── CHAP-DH（匿名 + 标准 DH）
```

---

## 九、参考文献

- CHAP‑IEM.md – 原始 ID 加密模式规范
- NIST SP 800‑38D – AES‑GCM 规范
- RFC 8439 – ChaCha20‑Poly1305

*文档版本：1.1*（中间人攻击章节修正）
