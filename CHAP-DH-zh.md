# CHAP-DH 技术文档

## Anonymous IEM Chain Protocol with DH Key Exchange

> **注意：本协议并非旧版的 Challenge-Handshake Authentication Protocol（挑战握手认证协议）。** 这是一个名称相同但完全不同的协议家族。CHAP-DH 是基于 DH 密钥交换的匿名 IEM 链协议。

---

## 一、概述

CHAP-DH 是 CHAP-IEM 协议的匿名变体。它移除了登录阶段，并将其替换为 DH 密钥协商。

| 对比维度 | CHAP | CHAP-IEM | CHAP-IEM-SKN | **CHAP-DH** |
|---------|------|----------|--------------|-------------|
| 预共享秘密 | 需要（密码） | 需要（密码） | 需要（Y） | **不需要** |
| 登录阶段 | 有 | 有 | 有 | **无（替换为 DH 协商）** |
| 身份验证 | 有 | 有 | 有 | **无（匿名）** |
| 服务端密钥表 | 需要 | 需要 | 需要 | **不需要** |
| ID₁ 生成方 | 服务端 | 服务端 | 服务端 | **任意一方（推荐客户端）** |
| IEM 链 | 有 | 有 | 有 | 有 |
| 前向安全性 | 不支持 | 支持 | 支持 | 支持 |
| 离线爆破风险 | 有 | 有 | 无 | **无** |

---

## 二、核心原理

### 2.1 设计理念

CHAP-DH 的本质是：**将 CHAP-IEM 的登录阶段替换为 DH 密钥协商**。

- 登录阶段的目的：建立初始共享密钥 K 并分发 ID₁
- DH 协商的目的：建立初始共享密钥 K
- ID₁ 的分发：通过 K 加密传输，可由任意一方发起

移除登录阶段意味着：
- 无用户名
- 无密码
- 无预共享秘密
- 完全匿名

### 2.2 颜色类比

```
无预共享秘密

客户端                              服务端
   |                                  |
私有随机数：红色                     私有随机数：蓝色
   |                                  |
橙色 = g^红色                        绿色 = g^蓝色
   |                                  |
   |-------- 交换橙色和绿色 ---------→|
   |←--------------------------------|
   |                                  |
棕色 = 绿色^红色                     棕色 = 橙色^蓝色
   = g^(红×蓝)                        = g^(红×蓝)
   |                                  |
   └────────── 相同的棕色 ────────────┘

棕色即为共享密钥 K
```

### 2.3 ID₁ 的可选发起方

与 CHAP-IEM 不同，CHAP-DH 中的 ID₁ 可以由**任意一方**生成并主动发起：

| 发起方 | 流程 | 适用场景 |
|--------|------|----------|
| 客户端 | 客户端生成 ID₁ → 用 K 加密发送 → 服务端解密并确认 | 默认推荐 |
| 服务端 | 服务端生成 ID₁ → 用 K 加密发送 → 客户端解密并确认 | 服务端主动建立会话 |

**推荐客户端发起**，因为客户端通常更了解自己的会话需求。

---

## 三、完整流程

### 3.1 密钥交换阶段

双方无需任何预共享秘密。

客户端生成密码学安全随机数 a，计算 A = g^a mod p，将 A 发送给服务端。

服务端生成密码学安全随机数 b，计算 B = g^b mod p，将 B 发送给客户端。

双方收到对方消息后，各自计算共享密钥 K：

- 客户端：K = B^a mod p
- 服务端：K = A^b mod p

此时双方拥有相同的会话密钥 K。

### 3.2 ID₁ 分发阶段（客户端发起模式）

客户端生成密码学安全随机数 ID₁，使用 K 加密后发送给服务端：

```
客户端 → 服务端：初始化包 = AES256_K(ID₁)
```

服务端用 K 解密，获得 ID₁，将其设置为当前有效 ID。

服务端可以返回任意内容（令牌、ID₂、主动推送消息等），用 ID₁ 加密：

```
服务端 → 客户端：响应包 = AES256_ID₁(任意数据 + ID₂)
```

其中：
- `任意数据`：可以是令牌、确认信息、主动推送消息等
- `ID₂`：服务端生成的下一个 ID

客户端用 ID₁ 解密，获得服务端返回的数据和 ID₂，将当前加密密钥更新为 ID₂。

**关键特点**：
- 响应包不一定必须是 "OK"
- 可以包含服务端主动推送的任何数据
- 第一个响应包就实现了双向通信

### 3.3 ID₁ 分发阶段（服务端发起模式）

服务端也可以主动发起：

```
服务端 → 客户端：初始化包 = AES256_K(ID₁)
```

客户端用 K 解密，获得 ID₁，将其设置为当前有效 ID。客户端用 ID₁ 加密响应返回：

```
客户端 → 服务端：响应包 = AES256_ID₁(任意数据 + ID₂)
```

### 3.4 正常 IEM 操作链

后续操作与 CHAP-IEM 完全一致。

任意一方使用当前 IDₙ 作为 AES256 密钥，对数据进行加密，发送给对方。对方用相同的 IDₙ 解密，执行操作或处理消息，生成新的随机 IDₙ₊₁，将结果与新 ID 打包，使用 IDₙ 加密后返回。

收到方用 IDₙ 解密，获得结果和新 IDₙ₊₁，将当前加密密钥更新为 IDₙ₊₁。

后续每次交互重复此流程，形成密钥链。

### 3.5 双向推送

由于 ID₁ 可以由任意一方发起，CHAP-DH 天然支持双向通信：

- 请求-响应模式：一方发起请求，另一方回复
- 推送模式：任一方主动发送数据，另一方解密并推进 ID 链

推送包格式与普通消息包完全相同：使用当前 IDₙ 加密，包含实际数据和 IDₙ₊₁。

### 3.6 异常恢复

当响应包丢失导致双方密钥不同步时：

一方使用自己认为的当前 IDₙ 发送消息。对方用期望的 IDₙ 解密成功，但发现 IDₙ 已失效（已被销毁）。

对方需要使用恢复密钥发送同步信息。在 CHAP-DH 中，恢复密钥为 DH 协商得到的 K（应保留用于异常恢复）。

恢复流程：
1. 对方用 K 加密当前有效 ID 和同步指令，发送给发送方
2. 发送方用 K 解密，获得当前有效 ID，更新本地状态
3. 发送确认包
4. 双方恢复正常通信

如果 K 已被废弃，则双方需要重新执行完整的 DH 交换和 ID₁ 分发流程。

---

## 四、工程实践指标

### 4.1 参数规格

| 参数 | 规格 | 说明 |
|------|------|------|
| p（模数） | 2048-4096 位 | 公开参数，可硬编码 |
| g（原根） | 通常为 2 或 5 | 公开参数，可硬编码 |
| a、b（随机数） | 256 位 | 必须使用 CSPRNG |
| K（DH 共享密钥） | 与 p 同量级 | 建议通过 KDF 派生为 AES 密钥 |
| IDₙ | 256 位 | 必须随机，每次交互更新 |
| AES 密钥 | 256 位 | 所有 AES 操作使用 AES-256 |

### 4.2 K 的处理

DH 交换得到的 K 是模幂结果，建议通过 KDF 派生为固定长度的 AES 密钥：

```
K_aes = HKDF-SHA256(K, "chap-dh-session")
```

### 4.3 服务端状态管理

CHAP-DH 属于**有状态**协议：

- 服务端需要为每个客户端维护当前有效 ID
- 但**不需要**维护用户密钥表（无预共享秘密）
- 每个会话的状态维护成本远低于登录时解密验证的成本

| 操作 | SKN（有登录） | CHAP-DH |
|------|--------------|---------|
| 建立会话 | 查密钥表 + 解密验证 | DH 交换（模幂） |
| 每次交互 | 无查表 | 无查表 |
| 服务端状态 | 会话 ID | 会话 ID |

### 4.4 性能对比

| 场景 | SKN | CHAP-DH |
|------|-----|---------|
| 首次建立会话 | 查表 + AES 解密 | DH 模幂计算 |
| 后续每次操作 | O(1) 状态查找 | O(1) 状态查找 |
| 多用户密钥表 | 需要（O(N) 空间） | **不需要** |
| 新增用户配置 | 需要预共享 Y | **零配置** |

---

## 五、安全分析

### 5.1 抗离线爆破

CHAP-DH 完全消除了离线爆破风险：

- 无密码参与
- 无预共享秘密
- K 来自 DH 协商，无任何可枚举目标

### 5.2 前向安全性

每次会话独立生成新的 a 和 b：

- K 仅用于当前会话
- ID 链本身也提供前向安全性（旧 ID 无法推导新 ID）
- 即使未来某次会话的 K 泄露，其他会话不受影响

### 5.3 匿名性

CHAP-DH 不传输任何身份信息：

- 无用户名
- 无证书
- 无任何可识别信息

### 5.4 中间人攻击

标准 DH 不抗中间人攻击。CHAP-DH 的定位是匿名通信，方案选择：

| 方案 | 说明 |
|------|------|
| 带外验证 | 用户手动比对密钥指纹 |
| 信任首次使用（TOFU） | 首次连接记录，后续验证 |
| 接受风险 | 匿名场景可接受 |

### 5.5 重放攻击

- DH 交换本身抗重放（每次 a、b 独立随机）
- IEM 链天然抗重放（ID 单次有效）

---

## 六、多用户场景分析

### 6.1 与 SKN 的对比

| 场景 | SKN | CHAP-DH |
|------|-----|---------|
| 单用户配置 | 需要预共享 Y | **无需配置** |
| 200 用户配置 | 200 个 Y，需密钥表 | **0 配置** |
| 新增用户 | 需要分配 Y | **即插即用** |
| 用户枚举攻击 | 可能 | **不适用（无用户概念）** |
| 服务端存储 | 密钥表 + 会话状态 | 仅会话状态 |

### 6.2 典型应用场景

**IoT 传感器网络**

- 成百上千个传感器需要连接网关
- 每个传感器预置独立密钥不现实
- CHAP-DH 允许传感器即插即用

**临时匿名访问**

- 访客 WiFi
- 临时设备配对
- 一次性安全通道

**服务端水平扩展**

- 无需共享密钥表
- 会话亲和性可选
- 重启不影响其他会话

---

## 七、与 CHAP-IEM 的关系

CHAP-DH 是 CHAP-IEM 的一个变体：

```
CHAP-IEM = 登录阶段（预共享 K + 服务端生成 ID₁）+ IEM 链
CHAP-DH  = DH 协商（无预共享）+ IEM 链（客户端或服务端生成 ID₁）
```

| 组件 | CHAP-IEM | CHAP-DH |
|------|----------|---------|
| 初始密钥来源 | 密码哈希 | DH 协商 |
| 身份验证 | 有（用户名） | 无 |
| ID₁ 生成方 | 服务端 | 任意一方 |
| IEM 链 | 有 | 有 |
| 异常恢复密钥 | K（密码哈希） | K（DH 协商） |
| 双向推送 | 支持 | 支持（天然） |

---

## 八、适用场景

**适合：**

- 大量匿名客户端的 IoT 场景
- 临时/一次性安全通信
- 服务端无密钥表架构
- 需要 IEM 链特性但无需身份验证
- 资源受限设备（DH 模幂可预计算）

**不适合：**

- 需要身份验证的场景
- 需要抗中间人攻击的敏感场景
- 已有 PKI 基础设施的环境

---

## 九、总结

CHAP-DH 的核心本质：

> **将 CHAP-IEM 的登录阶段替换为 DH 密钥协商，移除所有身份验证和预共享秘密，保留 IEM 链的全部安全特性。**

**协议层次关系：**

```
ZIM（锯齿交互模型）
  ├── CHAP（固定密钥 + ID 链 + 身份验证）
  ├── CHAP-IEM（ID 作为密钥 + 身份验证）
  ├── CHAP-IEM-SKN（预共享 Y + DH 混合 + 身份验证）
  └── CHAP-DH（标准 DH + IEM 链 + 匿名）
```

**核心优势：**

- 零预共享、零配置、零密钥表
- 完全匿名，不传输任何身份信息
- 继承 IEM 所有特性：链式密钥、前向安全、双向推送、异常恢复
- 服务端仅需维护会话状态，无需密码表
- 多用户场景下配置成本为零
