## Bidirectional Communication in CHAP Protocol Family

---

## 一、问题描述

CHAP、CHAP-IEM、CHAP-IEM-SKN 是否支持服务端主动推送消息？即不等待客户端请求，服务端主动向客户端发送数据。

---

## 二、核心结论

| 协议 | 支持双向推送 | 实现方式 |
|------|-------------|----------|
| CHAP | ✅ 支持 | 服务端使用预共享密钥 K 加密推送 |
| CHAP-IEM | ✅ 支持 | 服务端使用当前 IDₙ 加密，推送新 IDₙ₊₁ |
| CHAP-IEM-SKN | ✅ 支持 | 同 IEM，使用 K_session 或当前 IDₙ |

**整个 CHAP 家族都支持服务端主动推送。**

---

## 三、实现原理

### 3.1 核心假设的纠正

一个常见的误解是：服务端不知道客户端的当前 ID。

**实际情况：**
- 服务端知道客户端的当前 ID
- 因为当前 ID 就是服务端最后生成并发给客户端的那一个
- 服务端始终维护着"当前有效 ID"和"客户端应持有的 ID"

### 3.2 通用推送模式

无论哪种变体，服务端推送都遵循同一模式：

```
客户端                                服务端
   |                                    |
   | 当前 ID = IDₙ                       | 当前有效 ID = IDₙ
   | （双方最后同步的状态）               | （双方最后同步的状态）
   |                                    |
   |                                    | 有推送消息需要发送
   |                                    |
   |←------- 推送包（消息 + 新 ID）------|
   |                                    |
   | 用本地密钥解密                      |
   | 获得消息和下一个 ID                 |
   | 更新本地密钥/ID                     |
   |                                    |
   |（可选）客户端可发送确认包           |
   |-------- 确认包（确认 + 新 ID）-----→|
```

---

## 四、各协议具体实现

### 4.1 CHAP

**加密密钥**：预共享密钥 K（固定）

**推送包格式**：
```
推送包 = AES256_K(消息内容 + IDₙ₊₁)
```

**客户端处理**：
```
1. 用本地 K 解密
2. 获得消息和 IDₙ₊₁
3. 更新本地 ID 为 IDₙ₊₁
```

**特点**：
- K 固定不变，ID 链仍正常推进
- 推送不会影响后续通信的加密能力
- 异常恢复：使用 K 同步

### 4.2 CHAP-IEM

**加密密钥**：当前有效 IDₙ（随操作变化）

**推送包格式**：
```
推送包 = AES256_IDₙ(消息内容 + IDₙ₊₁)
```

**客户端处理**：
```
1. 用本地 IDₙ 解密（与服务器最后同步的 ID）
2. 获得消息和 IDₙ₊₁
3. 更新加密密钥为 IDₙ₊₁
```

**特点**：
- 推送被视为一次"操作"，推进 ID 链
- 推送后双方 ID 仍然同步
- 异常恢复：使用 K（预共享密钥）

### 4.3 CHAP-IEM-SKN

**加密密钥**：K_session（登录后）或当前 IDₙ（IEM 链启动后）

**推送包格式**：
```
推送包 = AES256_当前密钥(消息内容 + 新标识)
```

**客户端处理**：
```
1. 用本地当前密钥解密
2. 获得消息和新标识
3. 更新本地状态
```

**特点**：
- 登录完成前使用 K_session
- 登录完成后使用当前 IDₙ
- 异常恢复：使用 K_session

---

## 五、与客户端请求的对称性

### 5.1 请求-响应模式（客户端发起）

| 方向 | 发起方 | 加密密钥 | 包内容 | ID 更新 |
|------|--------|----------|--------|---------|
| 请求 | 客户端 | IDₙ | 操作指令 | 无 |
| 响应 | 服务端 | IDₙ | 结果 + IDₙ₊₁ | 客户端更新为 IDₙ₊₁ |

### 5.2 推送模式（服务端发起）

| 方向 | 发起方 | 加密密钥 | 包内容 | ID 更新 |
|------|--------|----------|--------|---------|
| 推送 | 服务端 | IDₙ | 消息 + IDₙ₊₁ | 客户端更新为 IDₙ₊₁ |
| 确认（可选） | 客户端 | IDₙ₊₁ | 确认标识 | 服务端确认 |

**完全对称。** 唯一的区别是谁发起第一次通信。

---

## 六、异常恢复

### 6.1 推送包丢失场景

```
客户端                                服务端
   |                                    |
   | 本地 ID = IDₙ                       | 当前有效 ID = IDₙ₊₁（已推进）
   |                                    |
   | 推送包丢失 ❌                       |
   |                                    |
   | 客户端发起下一次请求                |
   |-------- 请求包（使用 IDₙ）---------→|
   |                                    |
   |                                    | 用 IDₙ 解密成功
   |                                    | 但 IDₙ 已失效
   |                                    |
   |←-------- 恢复包 -------------------|
```

### 6.2 恢复机制

| 协议 | 恢复密钥 | 恢复包内容 |
|------|----------|-----------|
| CHAP | K | 当前有效 ID + 同步指令 |
| CHAP-IEM | K | 当前有效 ID + 同步指令 |
| CHAP-IEM-SKN | K_session | 当前有效 ID + 同步指令 |

恢复完成后，客户端更新本地状态，正常通信继续。

---

## 七、工程实现建议

### 7.1 推送确认机制

| 方案 | 说明 | 适用场景 |
|------|------|----------|
| 强制确认 | 客户端必须发送确认包 | 关键消息、需要可靠性 |
| 无确认 | 服务端不等待确认 | 非关键通知、允许丢失 |
| 重传机制 | 服务端保留推送包，超时后重传 | 需要可靠性的非关键消息 |

### 7.2 推送缓冲

服务端应维护推送队列：
- 客户端离线时缓存消息
- 客户端下次请求时携带缓冲消息
- 或使用独立推送通道

### 7.3 与请求的并发处理

可能同时发生的情况：
- 客户端发送请求的同时，服务端发起推送
- 此时双方可能使用相同的 IDₙ 加密

**解决方案**：
1. 请求和推送使用独立的 ID 链
2. 或使用锁机制确保串行处理
3. 或允许并发，通过超时重试解决冲突

---

## 八、与 WebSocket 的对比

| 特性 | CHAP 家族 | WebSocket |
|------|----------|-----------|
| 服务端主动推送 | ✅ 支持 | ✅ 原生支持 |
| 连接模型 | 请求-响应 + 推送 | 全双工长连接 |
| 状态管理 | ID 链（轻量） | 长连接状态 |
| 前向安全性 | ✅ 支持 | ❌ 通常不支持 |
| 消息丢失恢复 | ✅ 内置 | ❌ 需要应用层实现 |
| 实现复杂度 | 低 | 中 |
| 适用场景 | HTTP/类 HTTP 环境 | 实时双向通信 |

---

## 九、总结

### 9.1 核心结论

**CHAP 家族协议完全支持服务端主动推送。**

关键点：
- 服务端知道客户端的当前 ID
- 推送包使用当前 ID 加密，包含新 ID
- 客户端解密后更新本地状态
- 推送被视为一次"操作"，推进 ID 链

### 9.2 设计原则

推送包必须遵循的规则：
1. 使用双方最后同步的密钥/ID 加密
2. 必须包含新 ID，让客户端推进状态
3. 可选包含确认机制

### 9.3 适用范围

| 场景 | 是否适用 |
|------|----------|
| 服务端主动通知 | ✅ 适用 |
| 消息推送 | ✅ 适用 |
| 实时广播 | ⚠️ 需要多个独立会话 |
| 极高频率推送 | ⚠️ 建议评估 WebSocket |
